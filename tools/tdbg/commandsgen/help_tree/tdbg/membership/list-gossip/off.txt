NAME:
   tdbg membership list-gossip - List ringpop membership items

USAGE:
   tdbg membership list-gossip [command options]

OPTIONS:
   --role value  Membership role filter: all (default), frontend, history, matching, worker (default: "all")
   --help, -h    show help
