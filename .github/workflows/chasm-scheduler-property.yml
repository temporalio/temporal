name: CHASM Scheduler Property

on:
  pull_request:

permissions:
  contents: read

jobs:
  durable-scheduler-trace:
    name: Durable scheduler trace
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: ./.github/actions/prepare-go
      - run: make chasm-scheduler-durable-property-test
