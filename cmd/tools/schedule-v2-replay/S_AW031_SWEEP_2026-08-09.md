# s-aw031 Schedule V1 versus CHASM replay sweep

## Executive summary

This sweep read saved Schedule V1 workflow histories from `s-aw031` and replayed their external
inputs against fresh, in-memory CHASM schedulers. It did not update, signal, pause, migrate, or
delete a live schedule.

### Compatibility-policy rerun (2026-08-12)

After fixing CHASM immediate-trigger identity and introducing named shadow classifications, the
same 91 saved histories produce:

| Classification | Cases |
| --- | ---: |
| `match` | 16 |
| `timing_only` | 26 |
| `known_compatibility` | 8 |
| `significant` | 0 |
| `inconclusive` | 10 |
| `unsupported` | 31 |

The eight known cases retain `skip_deadline_boundary`; six also retain
`terminal_failure_propagation`. These are not discarded: each divergence carries its named
`knownDifference`, appears in report and cohort totals, and fails `--fail-on all`. The default
`significant` gate no longer treats the reviewed compatibility policy as a new V2 bug. The former
immediate-trigger boundary case is no longer significant because CHASM now honors the request's
identity timestamp.

After correcting carried-workflow identity mapping and replacing wall-clock timeouts with bounded
virtual work, the 91 saved histories classify as follows:

| Classification | Before | After | Interpretation |
| --- | ---: | ---: | --- |
| `match` | 16 | 16 | Compared action decisions, requests, counts, and final state match. |
| `timing_only` | 22 | 25 | The same workflow IDs were started at different observed times. |
| `significant` | 13 | 9 | A request, nominal occurrence, action count, or final state differs. |
| `inconclusive` | 0 reported / 9 external timeouts | 10 | Replay exceeded a virtual-work budget. |
| `unsupported` | 31 | 31 | The saved history lacks usable Schedule V1 initialization state. |

The identity fix cleared three previously significant, high-volume continue-as-new histories:
they now start the same 235, 285, and 306 workflows as V1 and differ only in observation time. A
fourth previously significant case is now correctly bounded as inconclusive. Investigation of the
nine remaining cases found three independent behavioral differences:

| Root cause | Cases | Determination | Impact |
| --- | ---: | --- | --- |
| Immediate trigger ignores `ScheduledTime` | 2 request cases; 1 action-changing boundary | V2 bug | Usually low; medium when receipt crosses a second boundary. |
| `SKIP` resolves at the exact deadline | 8 | Intended V2 semantics but significant migration incompatibility | Medium to high; several histories start roughly half as many workflows. |
| Terminal status becomes `ContinuedFailure` | 5 | Deliberate V2 behavior but observable migration incompatibility | Medium; the next workflow receives different input. |

The 36 `LastCompletionResult` differences and most later failure/action-count differences occur
only after an earlier unmatched workflow ID. They are downstream comparisons of different
execution chains, not additional bugs.

Production identifiers are omitted. Case references below are stable truncated hashes from the
redacted rerun. Raw histories, the unredacted report, and the identity crosswalk remain outside the
repository in an owner-only temporary directory.

## Scope

| Stage | Result |
| --- | ---: |
| Namespaces listed | 662 |
| Namespaces containing schedules | 52 |
| Schedule rows listed | 284 |
| Saved legacy candidates | 69 schedules / 91 unique histories |
| Histories without V1 initialization state | 31 |
| Replayable V1 histories | 60 histories / 38 schedules / 19 namespaces |

The collector now de-duplicates schedule IDs before sampling, so repeated visibility rows cannot
consume multiple sample slots in future sweeps. These ratios are cohort-oriented observations,
not fleet-wide prevalence estimates.

## Fidelity corrections and what they changed

### Carried workflow/run identity

A continued V1 scheduler run can begin with workflows already running. Migration-state conversion
preserves those workflow and run IDs in CHASM buffered starts, but the old oracle initialized its
identity map as empty and learned identities only from new starts. It therefore could not apply a
completion for a workflow carried across the run boundary. Under `SKIP` or buffering policies, the
apparently running workflow then blocked later actions and created a false missing-action cascade.

The replay now seeds exact workflow/run mappings from incomplete migrated buffered starts. New
starts are also mapped by workflow and run ID rather than workflow ID alone. A completion with no
run ID is accepted only if it has exactly one candidate; ambiguity is reported as inconclusive.

This correction moved three production histories from significant to timing-only. It does not
force a V1 completion onto a different CHASM nominal occurrence: if CHASM genuinely did not emit
the corresponding workflow ID, the completion remains pending and the occurrence difference is
retained.

### Chronological start attempts

V1 can make several immediate-start attempts with the same whole-second workflow ID, with one
success interleaved among duplicate-ID failures. The old replay kept successes and failures in
separate queues and returned every failure before any success for that ID. Replay now consumes a
single history-ordered attempt stream.

The corrected rerun changes the high-rate trigger case from a misleading missing/duplicate pair
with equal totals to one extra CHASM success and one unmatched V1 failed attempt. The surviving
difference aligns with a trigger stamped just before a second boundary and delivered just after
it; it is product behavior, not attempt-order contamination.

### Deterministic replay budgets

The original sweep killed nine histories after 15 wall-clock seconds. That measured machine speed
and left no structured evidence. Replay now defaults to per-history limits of 10,000 virtual
deadlines, 100,000 CHASM tasks, and 10,000 starts. All ten bounded cases reached 10,001 deadlines;
nine produced no actions and one produced a single matching action before the limit. They are
`inconclusive/replay_budget_exceeded`, with counters, and do not receive fabricated missing-action
findings from partial output.

The ten cases belong to one namespace and appear to combine dense schedules with a long sparse
history horizon. Raising the limit is possible for targeted profiling, but is unnecessary for a
broad sweep and can make runtime scale with virtual occurrences rather than history size.

### Payload-safe request diagnostics

Report version 3 explains request mismatches without decoding customer payloads. It records field
presence, element counts, payload encodings, reserved scheduled-time values, failure-info types,
and deterministic digests in the restricted unredacted report. Default redaction removes digests
and identifiers. Raw payload values and failure messages are never copied into either report.

## Remaining findings

### Immediate trigger identity and scheduled-time precision

One case has 57 `search_attributes` mismatches. All 57 are
`TemporalScheduledStartTime`: V1 records a whole-second value while CHASM retains a sub-second
value. Two more instances occur in another significant case. This confirms in production data the
controlled-corpus behavior documented in [DIVERGENCES.md](DIVERGENCES.md).

The cause is stronger than a formatting difference. `TriggerImmediately.ScheduledTime` is
documented as the timestamp used for target-workflow identity. V1 reads it; CHASM initializes its
immediate backfiller from `ctx.Now` and never reads it. In the action-changing case, the request was
stamped at `23:33:03.998` and observed by the scheduler at `23:33:04.003`. V1 therefore targeted
the `23:33:03` workflow ID while CHASM targeted `23:33:04`, changing duplicate-ID rejection and
producing 57 rather than 56 successful starts.

First-pass triage:

- **Compatibility impact: Low normally, Medium at a second boundary.** Workflow code and
  visibility can observe the precision change; a boundary crossing changes action identity.
- **V2 bug likelihood: High.** Ignoring an explicit identity timestamp is inconsistent with the
  request contract.
- **Confidence: High.** The safe diagnostic decodes only the reserved datetime field.

### Completion result and continued failure propagation

Across significant cases, request diagnostics report:

| Field | Mismatching starts | Observed structure |
| --- | ---: | --- |
| `last_completion_result` | 36 | Presence/count or deterministic payload digest differs. |
| `continued_failure` | 750 | Failure presence or top-level failure-info type differs. |
| `search_attributes` | 59 | All identified safe differences are scheduled-time precision. |

The 750 continued-failure count is dominated by three chained interval histories: 719 requests
carry a CHASM timeout failure where V1 carries none. This is not 719 independent defects. Once the
first nominal occurrence diverges, a V1 completion cannot be applied to a CHASM workflow that was
never started; CHASM retains its own prior outcome, and later request comparisons inherit that
state. Similar causal contamination applies to later missing-action and action-count findings.

The first timeout/termination mismatch in five histories is independent and confirmed by unit
tests. V1's response builder attaches a failure only for `FAILED`; canceled, terminated, and timed
out responses carry status but no failure, so V1 leaves `ContinuedFailure` unchanged. CHASM's
workflow callback creates typed failures for all three statuses and the invoker sends that failure
to the next workflow. Twenty activity-failure mismatches, eight reverse activity-failure
mismatches, and all 36 last-completion-result mismatches occur after an unmatched occurrence and
are downstream chain differences.

First-pass triage:

- **Compatibility impact: Medium.** Completion input can change workflow behavior.
- **V2 bug likelihood: Low.** CHASM exposes more complete callback information and behaves
  consistently, but exact V1 migration compatibility is not preserved.
- **Confidence: High.** V1 response-builder tests, CHASM invoker tests, and production histories
  agree on the terminal-status matrix.

### Nominal occurrence selection

All nine significant histories retain an action-level or request-level difference. Eight contain
missing or extra workflow IDs caused by deadline or trigger identity. In seven of them, an
unmatched completion is followed by a lower CHASM action count; the first missing/extra pair is the
candidate and subsequent differences may be its cascade. The pattern is consistent with the
controlled `SKIP` boundary difference: V1 generates elapsed occurrences during a delayed workflow
task, while CHASM resolves overlap at exact virtual deadlines.

One illustrative case has one V1 workflow at nominal `01:30` and one CHASM workflow at `03:00`.
The V1 completion cannot safely be applied to the different CHASM execution. Retaining it as
unapplied is intentional oracle behavior, not the old carried-identity bug.

First-pass triage:

- **Compatibility impact: Medium to High.** A different nominal occurrence changes workflow ID
  and cadence. Three one-minute histories and two daily histories start approximately half as many
  workflows under CHASM replay.
- **V2 correctness-bug likelihood: Low.** Exact-deadline `SKIP` matches the public overlap-policy
  contract; V1 admits the elapsed occurrence because completion is refreshed later in the same
  workflow task.
- **Confidence: High.** The controlled `skip-running` corpus, invoker boundary tests, and all eight
  production timelines show the same ordering.

## Significant-case matrix after correction

| Redacted case | Cohorts | V1 starts | CHASM starts | V1/CHASM action count | Principal evidence |
| --- | --- | ---: | ---: | ---: | --- |
| `9f7aa16059e5` | SKIP, pause, start failure | 56 | 57 | 422 / 423 | CHASM ignores trigger `ScheduledTime`; boundary changes workflow ID and success outcome. |
| `be4c2c16d9a9` | SKIP, calendar | 110 | 18 | 599 / 507 | First unmatched occurrence; continued-failure cascade. |
| `ca50c0c4e2b7` | SKIP, calendar, pause | 81 | 42 | 81 / 42 | Completion-result/failure and occurrence cascade. |
| `4b81237c6c2d` | SKIP, interval, pause | 1 | 1 | 2 / 2 | One-for-one nominal occurrence replacement. |
| `10879b724e04` | unspecified, calendar, pause, update | 5 | 3 | 5 / 3 | One extra and three missing occurrences. |
| `7a94bceff7b1` | SKIP, interval | 443 | 221 | 84,261 / 84,039 | First unmatched occurrence; timeout-failure cascade. |
| `66d92e013b3d` | SKIP, interval | 499 | 250 | 83,818 / 83,569 | First unmatched occurrence; timeout-failure cascade. |
| `dbbc6e14dcab` | SKIP, interval | 499 | 250 | 83,319 / 83,070 | First unmatched occurrence; timeout-failure cascade. |
| `5a9061306d68` | unspecified, interval, pause, update | 8 | 6 | 8 / 6 | Termination-failure and occurrence cascade. |

## Overall determination

The corrected harness removes the known identity and attempt-order false positives and makes the
remaining evidence actionable. One implementation bug is identified with high confidence: CHASM
does not honor `TriggerImmediately.ScheduledTime`. The `SKIP` result is not a V2 correctness bug,
but it is not minor migration noise either; production examples show cadence reductions near 50%.
Terminal failure propagation is also a real, medium-impact input compatibility difference, though
CHASM's richer failure is internally correct.

The recommended product work is therefore: fix immediate-trigger identity first; make an explicit
migration-policy decision for `SKIP`; and decide whether exact V1 compatibility requires suppressing
canceled/terminated/timeout `ContinuedFailure`. The derived result/failure cascades need no separate
fix unless they survive after the first action divergence is resolved.
