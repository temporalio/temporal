build:
  roots:
    - internal
    - api
lint:
  ignore:
    - temporal/api # do not lint public api
    - dependencies
  use:
    - DEFAULT
  except:
    - PACKAGE_SAME_JAVA_MULTIPLE_FILES
    - PACKAGE_SAME_JAVA_PACKAGE
