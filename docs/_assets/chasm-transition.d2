# https://d2lang.com
# Generate SVG by running `make` inside of `docs/`.

vars: {
  d2-config: {
    layout-engine: dagre
  }
}

direction: down

*.style.bold: true

Event -> "Transition Fn"
"Transition Fn" -> Fields: "write"
"Transition Fn" -> Tasks: "schedule"
